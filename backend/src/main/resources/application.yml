spring:
  application:
    name: history-creation-backend
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/history_creation?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&connectionCollation=utf8mb4_unicode_ci
    username: root
    password: 123456
  mvc:
    async:
      request-timeout: 180000

server:
  port: 8080
  servlet:
    context-path: /api
  # SSE 长连接需要关闭响应缓冲
  tomcat:
    connection-timeout: 180000

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

# JWT
jwt:
  secret: history-creation-jwt-secret-key-min-256-bits-for-hs256
  expiration-ms: 86400000

# 讯飞星火大模型（HTTP接口）
spark:
  app-id: ca8279b2
  api-key: xEcMmGZZABmuDnywKmvA:PBaGFyyZGLZeucaahpkM
  api-secret: YjkzYjcyYWYxNmVmMGRhNzY2YjYwMjFm
  # HTTP接口地址（Spark X2）
  api-url: https://spark-api-open.xf-yun.com/x2/chat/completions
  # 模型版本
  model: spark-x
  # 最大token数
  max-tokens: 8192
  # 超时秒数
  timeout-seconds: 60
